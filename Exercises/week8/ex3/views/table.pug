extends layout

block content
    div.container.d-flex.align-items-center.justify-content-center 
        table.table.table-striped(style="width: 40%")
            tr
                th ID
                th Name
                th Age
                th Edit
                th Delete

            each item in items
                tr
                    td= item.ID
                    td= item.Name
                    td= item.Age
                    td
                        a(href=`/edit-student/${item.ID}`, class="btn btn-info") Edit
                    td
                        form(action=`/delete-user/${item.ID}`, method="post")
                            button(type="submit" class="btn btn-danger") Delete
                        
    div.mt-3(style="text-align: center")
        a(href="/" class="btn btn-primary") Go Back to Home

